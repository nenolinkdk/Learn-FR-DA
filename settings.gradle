pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Learn-FR-DA"
include ":app", ":engine", ":app-shell"
project(":engine").projectDir = file("vendor/nenoling-app/template/android/engine")
project(":app-shell").projectDir = file("vendor/nenoling-app/template/android/app-shell")
