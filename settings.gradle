pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Learn-FR-DA"
include ":app", ":nenoling-engine", ":nenoling-shell"
project(":nenoling-engine").projectDir = file("vendor/nenoling-app/template/android/engine")
project(":nenoling-shell").projectDir = file("vendor/nenoling-app/template/android/app-shell")
